[project]
name = "rag-master"
version = "0.1.0"
description = "Paragraph search service based on Flask + Qdrant + E5 embeddings"
requires-python = ">=3.11"
dependencies = [
    "flask",
    "pandas",
    "qdrant-client",
    "torch",
    "torchaudio",
    "torchvision",
    "tqdm",
    "transformers",
]

[tool.uv]
package = false
